import os
os.system("pip install -r requirements.txt")
# El run.py en esta entrega no es necesario, pero se incluye para que el usuario pueda instalar las dependencias necesarias para correr el proyecto ya que todo está en jupyter notebook.